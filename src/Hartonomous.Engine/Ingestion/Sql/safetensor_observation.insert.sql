INSERT INTO substrate.safetensor_observation (
    model_source_id,
    context_type_id,
    attestation_type_id,
    edge_type_id,
    edge_hash,
    score,
    weight,
    tensor_hash,
    package_tensor_hash,
    source_tensor_name,
    primitive_code,
    tuple_code,
    slot_code,
    modality_code,
    layer_index,
    head_index,
    expert_index,
    adapter_name,
    fused_slice
)
SELECT
    t.model_source_id,
    t.context_type_id,
    t.attestation_type_id,
    t.edge_type_id,
    t.edge_hash,
    t.score,
    t.weight,
    t.tensor_hash,
    t.package_tensor_hash,
    t.source_tensor_name,
    t.primitive_code,
    t.tuple_code,
    t.slot_code,
    t.modality_code,
    t.layer_index,
    t.head_index,
    t.expert_index,
    t.adapter_name,
    t.fused_slice
FROM unnest(
    @model_source_ids,
    @context_type_ids,
    @attestation_type_ids,
    @edge_type_ids,
    @edge_hashes,
    @scores,
    @weights,
    @tensor_hashes,
    @package_tensor_hashes,
    @source_tensor_names,
    @primitive_codes,
    @tuple_codes,
    @slot_codes,
    @modality_codes,
    @layer_indexes,
    @head_indexes,
    @expert_indexes,
    @adapter_names,
    @fused_slices
) AS t(
    model_source_id,
    context_type_id,
    attestation_type_id,
    edge_type_id,
    edge_hash,
    score,
    weight,
    tensor_hash,
    package_tensor_hash,
    source_tensor_name,
    primitive_code,
    tuple_code,
    slot_code,
    modality_code,
    layer_index,
    head_index,
    expert_index,
    adapter_name,
    fused_slice
);
